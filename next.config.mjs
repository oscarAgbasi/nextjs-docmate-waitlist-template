/** @type {import('next').NextConfig} */
// .env, .env.local, .env.development, .env.production are loaded from project root
// and exposed as process.env in server code (API routes, server components).
// Restart the dev server after changing .env files.
const nextConfig = {}

export default nextConfig
